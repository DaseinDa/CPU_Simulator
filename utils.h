#include <iostream>
#include <fstream>
#include <string>
#include <regex>
#include <vector>
#include <unordered_map>

// 支持的指令列表
// const std::vector<std::string> INSTRUCTIONS = {
//     "fld", "fsd", "add", "addi", "slt", "fadd", "fsub", "fmul", "fdiv", "bne"
// };

// void parseAssemblyLine(const std::string& line, std::string& currentLabel, bool& inLabelScop);